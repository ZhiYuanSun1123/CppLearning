# 练习1：让测试阶段失败并恢复

1. 在 `tests/audio_metadata_test.cpp` 中临时把 `2.5` 改成 `3.0`。
2. 提交并推送实验分支，观察四个 build-and-test matrix 项失败。
3. 点开一个失败项，定位 Expected、Actual、文件和行号。
4. 恢复 `2.5`，再次提交并推送，确认检查恢复绿色。
5. 将两个提交链接和观察结果写入 `report/学习报告.md`。

不要在主分支长期保留故意失败代码。
